const calculateArea = (a: number, b: number): number => a * b;

const Length = 20;
const breadth = 5;

const area = calculateArea(Length, breadth);

console.log("Area of the rectangle:", area);
